============
Installation
============

At the command line::

    $ easy_install mdvoxelclustering

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv mdvoxelclustering
    $ pip install mdvoxelclustering
